{
  "ConnectionStrings": {
    "IdentityConnection": "Host=${DB_HOST};Database=${DB_NAME_IDENTITY};Port=5432;Username=${DB_USER};Password=${DB_PASSWORD}",
    "LearningResourceConnection": "Host=${DB_HOST};Database=${DB_NAME_LEARNING};Port=5432;Username=${DB_USER};Password=${DB_PASSWORD}"
  }
,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
